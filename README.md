1. Create a Docker file for Frontend either index.html/react app/ js
2. Build the Docker to install dependncies <docker build -t frontend:v1 .>
3. Run the the container <docker run -d -p 8080:80 --name frontend frontend:v1>

